#include "main.h"
#include <stdio.h>
/**
 * mul - function to multiply numbers
 * @a: first number
 * @b: second number
 * Return: returns the result
 */

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}

